cc.Class({
    extends: cc.Component,

    properties: {
        number: 0,
    },

    // use this for initialization
    onLoad: function () {
    },

    // called every frame
    update: function (dt) {

    },
});
